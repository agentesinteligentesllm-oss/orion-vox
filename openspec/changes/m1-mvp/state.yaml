change_id: m1-mvp
title: "M1 - Base segura funcional Orion Vox"
status: in-progress  # draft | in-review | in-progress | completed | archived
owner: orion-vox
target_milestone: M1
created: 2026-05-01
last_updated: 2026-05-03 (Wave 13)
dependencies: []  # ADR-012 fue creado en Wave 1; ya no es dependencia bloqueante
completed_blocks:
  - B0  # 45b0707 — setup base Svelte 5 + Vite 7 + TS + Tailwind 4 + Biome + PWA
  - B1  # c07b235 — Supabase backend (código completo; deploy pendiente en B8)
  - B2  # 138f4e3 — PWA Auth & Config (auth store, routing, settings, logout)
  - B3-voice  # 5ebb458 — Voice (VoiceInputController, TtsOutputController, VoiceScreen)
  - B4.1  # d1e8a94 — plan-intent-client.ts + tests + audit fixes ES + Biome
  - B4.2  # b959081 — VoiceScreen integra plan-intent (loading, cards, 14 error msgs)
  - B4.3  # e5bb9ff — PlanPreview.svelte (render legible humano) + 10 tests unit
  - B4.4  # ae1ce17 — clarification flow (TTS + re-listen + buildClarifiedPrompt) + 10 tests
  - B4.5  # 7299218 — 5 tests E2E flow completo voice→plan-intent→PlanPreview
  - B5    # 3c3b926 — ConfirmationModal + confirmation-utils + execute-plan-client stub + VoiceScreen + 2 tests E2E
  - B6    # 496e848 — executePlan() real + AuditView + redact-client + VoiceScreen wired + 2 tests skip (ADR-014)
  - B7    # d46a9c9 — manifest canónico + Workbox prompt + NetworkOnly + pwa.svelte.ts + banners + iconos + 2 tests E2E
current_block: B8  # Deploy + Smoke E2E Cubot KK9
next_block: ~  # B8 es el bloque final de M1
artifacts:
  proposal: proposal.md
  spec: spec.md
  design: design.md
  tasks: tasks.md
acceptance_criteria:
  - "Login Supabase Auth funciona (magic link recibido, sesion persistente)"
  - "Edge plan-intent recibe prompt + JWT, llama Gemini server-side, devuelve Plan JSON o Clarification"
  - "Edge execute-plan ejecuta Plan JSON con allowlist server, audita, devuelve resultado"
  - "Edge schema-summary autogenerada, filtrada por ORION_ALLOWED_TABLES, redactada por ORION_REDACTED_COLUMNS"
  - "JWT invalido / user.id distinto rechazado con 401, intento auditado"
  - "Smoke E2E voz->Supabase exitoso en Cubot KK9 (post Web Speech smoke)"
  - "Confirmacion tactil bloquea writes hasta tap explicito"
  - "Operaciones DDL bloqueadas server-side y reportadas con error legible"
  - "Allowlist server rechaza tablas no listadas con 403"
  - "Politica de redaccion ORION_REDACTED_COLUMNS aplica en sql_params, result_summary y TTS"
  - "Auditoria 100% cobertura: ok, error, bloqueado, rechazado por usuario"
  - "Deploy live accesible HTTPS e instalable como PWA en Cubot KK9"
  - "Todas las US M1 marcadas como done"
related_adrs:
  - ADR-001  # Plan F+ arquitectura
  - ADR-003  # Plan JSON
  - ADR-004  # service_role server-side M1, rol dedicado M2
  - ADR-005  # Gemini key server-side desde M1 (reescrito)
  - ADR-006  # PWA pura
  - ADR-007  # Web Speech API
  - ADR-008  # auditoria server-side desde M1
  - ADR-009  # roadmap modular (reescrito post auditoria Codex)
  - ADR-010  # schema autogeneracion
  - ADR-012  # framework PWA Svelte 5 + Vite + TS (creado Wave 1)
  - ADR-013  # shared plan-schema entre PWA y Deno sin monorepo (B1)
notes: |
  Wave 13 (2026-05-03): B7 cerrado. 224/224 Vitest verde + 2 skip. Gates todos verdes.

  B0-B7 completos (~98% M1). Próximo: B8 (Deploy + Smoke E2E Cubot KK9).

  Orden real de implementación: B0→B1→B2→B4-Voice→B4.1→B4.2→B4.3→B4.4→B4.5→B5→B6→B7.

  Decisión divergencia B7 resuelta: Workbox (tasks T7.2, ya instalado en B0) sobre
  SW vanilla (spec §7 letra). Spirit de spec cumplido con runtimeCaching NetworkOnly
  explícito para Supabase + Gemini.

  T7.4/T7.5 N/A: spec §7 + ADR-006 los excluyen (requieren nativo Kotlin).

  Fix Vitest B7: virtual:pwa-register aliaseado a src/__mocks__/pwa-register.ts
  en vitest.config.ts. Necesario porque vite-plugin-pwa registra módulo virtual
  que no resuelve en entorno Node/jsdom.

  Iconos en public/icons/ son placeholders (fondo violeta #863bff). Reemplazar
  con PNGs reales pre-launch en B8/post-M1.

  Riesgo activo: deno test de Edge Functions no re-verificado desde T1.6.
  Re-verificación obligatoria en B8 pre-deploy. Supabase/functions/ sin cambios
  en B2-B7 (riesgo bajo).

  Pendiente crítico para B8: director debe completar setup Supabase (§9 HANDOFF):
  proyecto, magic link, migrations, env vars Edge, deploy funciones.

  Deuda activa M1→M2: TD-001-bis, TD-003, TD-004, TD-005, TD-008.

  2 tests B6 skip: tests/e2e/b6-execute-plan-flow.test.ts — se activan
  cuando .env.local tenga VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY.

  Gotchas acumulados B5-B7 (importantes para B8):
  - handleConfirmed() es async: planResponse=null ANTES de executePlan.
  - voiceState reset manual a 'idle' en finally (no depender de resetToIdle).
  - Mock execute-plan-client en b53: incluye ExecutePlanClientError + executePlan.
  - confirmSettings: lazy-load en callPlanIntent, NO en $effect (deadlock IDB).
  - recognition.resetToIdle() es no-op en el mock → emit(rec,'state','idle').
  - tts.cancel() emite onerror con code:'interrupted', NO onend.
  - toBeDisabled() no disponible (sin jest-dom) → usar .disabled === false.
  - virtual:pwa-register: siempre mockear en tests que monten App.svelte.
